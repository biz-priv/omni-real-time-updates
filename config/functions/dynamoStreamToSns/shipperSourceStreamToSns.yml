---
function:
  name: omni-wt-shipper-source-ddb-to-sns-${self:provider.stage}
  handler: src/dynamoStreamToSns/shipperSourceStreamToSns.handler
  # provisionedConcurrency: 1
  # concurrencyAutoscaling: true
  layers:
    - { Ref: OmniRealTimeReplicationLayerLambdaLayer }
  package:
    patterns:
      - "!**/*"
      - "src/dynamoStreamToSns/shipperSourceStreamToSns.js"
      - "src/shared/**"
  events:
    - stream: ${ssm:/omni-wt-rt-updates/${self:provider.stage}/shipper-source/ddb.streamArn~true}
  environment:
    # SNS_TOPIC_ARN: ${ssm:/omni-wt-rt-updates/${self:provider.stage}/shipper-source/sns.arn~true}
    SNS_TOPIC_ARN: arn:aws:sns:us-east-1:332281781429:omni-wt-rt-shipper-source-header-dev
